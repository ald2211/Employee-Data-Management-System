import EmployeeManagementSystem from "../components/EmployeesList"

const EmployeeManagementPage = () => {
  return <EmployeeManagementSystem />;
}

export default EmployeeManagementPage

// page created to breakdown to smaller components for better readability and maintainability.due to lack of time i have not broken down further to smaller components like search bar, employee list, pagination etc.